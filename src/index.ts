// Public API surface. Keep exports explicit to avoid leaking internals.
export { NetworkManager } from './core/manager';
export { Network } from './core/types';
export type { Rule, Node } from './core/types';
